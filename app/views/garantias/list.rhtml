<h1 class="centrado">Garantias</h1>
<!--inicia ajax -->
<label for="searchtext">Búsqueda por nombre:</label>
  <%= text_field_tag :searchtext %>
  <%= observe_field(:searchtext,
                 :frequency => 0.25,
                 :update => :search_hits,
                 :with  => "searchtext",
                 :url => { :action => :live_search }) %>
<p>Resultados de la Busqueda:</p>
<div id="search_hits">
  <div class="scroll">
    <table>
      <tr>
      <% for column in Garantia.content_columns %>
        <th><%= column.human_name %></th>
      <% end %>
        <th colspan="3">Acciones</th>
      </tr>
  
      <% for garantia in @garantias %>
      <tr>
        <% for column in Garantia.content_columns %>
        <td><%=h garantia.send(column.name) %></td>
        <% end %>
        <td><%= button_to 'Detalles', :action => 'show', :id => garantia %></td>
        <td><%= button_to 'Editar', :action => 'edit', :id => garantia %></td>
        <td><%= button_to 'Eliminar', { :action => 'destroy', :id => garantia }, :confirm => 'Estas seguro?', :method => :post %></td>
      </tr>
      <% end %>
    </table>
  </div>
</div>

<h4><%= button_to 'Agregar Garantia', :action => 'new' %></h4>
