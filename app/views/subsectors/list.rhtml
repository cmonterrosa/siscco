<h1 class="centrado">Subsectores</h1>
<!--inicia ajax -->
<label for="searchtext">Búsqueda por subsector:</label>
  <%= text_field_tag :searchtext %>
  <%= observe_field(:searchtext,
                 :frequency => 0.25,
                 :update => :search_hits,
                 :with  => "searchtext",
                 :url => { :action => :live_search }) %>
<p>Resultados de la Busqueda:</p>
<div id="search_hits">
  <div class="scroll">
    <table>
      <tr>
      <% for column in Subsector.content_columns %>
        <th><%= column.human_name %></th>
      <% end %>
        <th colspan="3">Acciones</th>
      </tr>
  
      <% for subsector in @subsectors %>
      <tr>
        <% for column in Subsector.content_columns %>
        <td><%=h subsector.send(column.name) %></td>
        <% end %>
        <td><%= button_to 'Detalles', :action => 'show', :id => subsector %></td>
        <td><%= button_to 'Editar', :action => 'edit', :id => subsector %></td>
        <td><%= button_to 'Eliminar', { :action => 'destroy', :id => subsector }, :confirm => 'Estas seguro?', :method => :post %></td>
      </tr>
      <% end %>
     </table>
  </div>
</div>

<h4><%= button_to 'Agregar Subsector', :action => 'new' %></h4>
