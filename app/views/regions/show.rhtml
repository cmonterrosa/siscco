<% for column in Region.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @region.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @region %> |
<%= link_to 'Back', :action => 'list' %>
